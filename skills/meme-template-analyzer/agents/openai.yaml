interface:
  display_name: "Meme Template Analyzer"
  short_description: "Analyze memes into JSON templates and render prompts"
  default_prompt: "Use $meme-template-analyzer to analyze this meme into a reusable JSON template and prompt-generation contract."
